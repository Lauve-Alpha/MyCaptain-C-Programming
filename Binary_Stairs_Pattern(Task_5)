#include<stdio.h>
int main()
{
	int i, j;
  //outer loop for the number of lines
	for (i = 1 ; i <= 6; i++)
	{
    //initializing variable for while loop
		j = 1;
    //loop to print '01's
		while (j <= i)
		{
			printf("01");
			j++;
		}
		printf("\n");
	}
	return 0;
}
